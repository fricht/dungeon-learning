# dungeon-learning

A c++ PPO project.

The goal is to train a neural network to play paper dungeon.

## Compile and execute

### Linux

> Requires cmake, make and a c++ compiler

```cmd
cd build
cmake ..
make
./main
```
